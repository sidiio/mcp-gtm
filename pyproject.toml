[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gtm-mcp"
version = "0.1.0"
dependencies = [
    "mcp>=1.0.0",
    "google-api-python-client>=2.0.0",
    "google-auth>=2.0.0"
]

[project.scripts]
gtm_mcp = "gtm_mcp:main"
